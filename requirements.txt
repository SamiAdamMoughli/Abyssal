# ===========================================================================
# Mission Radar (Spyhop) — Production Dependencies
# Python 3.12+
# ===========================================================================

# --- Web framework -----------------------------------------------------------
fastapi[standard]==0.115.6
uvicorn[standard]==0.32.1
uvloop==0.21.0
httptools==0.6.4
sse-starlette==2.2.1

# --- Async HTTP client (for tasks that call external APIs) -------------------
httpx==0.28.1
websockets==16.0

# --- Database (async + sync) -------------------------------------------------
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
geoalchemy2==0.15.2
alembic==1.14.0

# --- Data validation ---------------------------------------------------------
pydantic[email]==2.10.3
pydantic-settings==2.7.0
annotated-types==0.7.0

# --- Cache / Broker ----------------------------------------------------------
redis[hiredis]==5.2.1
celery[redis]==5.4.0
flower==2.0.1

# --- Resilience (exponential backoff) ----------------------------------------
tenacity==9.0.0

# --- Serialization (faster JSON) ---------------------------------------------
ujson==5.10.0
orjson==3.10.12

# --- Structured logging -------------------------------------------------------
structlog==24.4.0

# --- Geospatial ---------------------------------------------------------------
shapely==2.0.6
pyproj==3.7.0

# --- Utilities ----------------------------------------------------------------
python-dotenv==1.0.1
requests==2.32.3

# --- Testing ------------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-benchmark==4.0.0
anyio[trio]==4.7.0

# --- Type checking (dev) ------------------------------------------------------
# mypy==1.13.0
# types-requests==2.32.0
