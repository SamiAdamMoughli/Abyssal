# Backend API base URL.
# Development: leave empty — Vite proxies /api/* to http://127.0.0.1:8000 (see vite.config.js).
# Production:  set to your deployed API origin, e.g. https://api.your-domain.com
VITE_API_URL=
